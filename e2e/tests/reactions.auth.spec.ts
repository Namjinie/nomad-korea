import { test } from '@playwright/test'

test.describe('반응 (로그인)', () => {
  test.fixme('RA-01: 좋아요 클릭 시 카운트가 증가하고 버튼이 활성화 상태가 된다', async () => {})
  test.fixme('RA-02: 좋아요 재클릭(취소) 시 카운트가 원래대로 돌아온다', async () => {})
  test.fixme('RA-03: 좋아요 → 싫어요 전환 시 각 카운트가 올바르게 변경된다', async () => {})
})
